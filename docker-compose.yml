# docker-compose removed — this repository now runs locally without Docker or Swarm.
# The original docker-compose file has been intentionally purged. If you see a placeholder
# here, it is only to inform users and can be safely deleted.

